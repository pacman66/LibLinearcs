using System;

namespace liblinearcs {
    class Program
    {        
        static void Main(string[] args)
        {
            App app = new App();
            // run app
            app.Run(args);
        }
    }
}
