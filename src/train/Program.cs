using System;
using liblinear;

namespace liblinear.train {
    class Program
    {        
        static void Main(string[] args)
        {
            App app = new App();
            // run app
            app.Run(args);
        }
    }
}
